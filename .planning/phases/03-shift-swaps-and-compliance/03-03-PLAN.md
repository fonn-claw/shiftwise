---
phase: 03-shift-swaps-and-compliance
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/dal/compliance.ts
  - src/components/compliance/compliance-status.tsx
  - src/components/compliance/premium-pay-card.tsx
  - src/components/compliance/audit-log.tsx
  - src/app/(dashboard)/compliance/page.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "Compliance dashboard shows predictive scheduling compliance status with clear indicators"
    - "Warning displayed when schedule is posted less than required notice period"
    - "Premium pay calculator shows affected shifts and additional cost for last-minute changes"
    - "Audit log displays chronological list of all schedule changes with timestamp, who, and what"
    - "Audit log is filterable by date range, employee, and action type"
  artifacts:
    - path: "src/lib/dal/compliance.ts"
      provides: "Data access for compliance status, audit log, premium pay"
      exports: ["getComplianceStatus", "getAuditLog", "getPremiumPayChanges"]
    - path: "src/components/compliance/compliance-status.tsx"
      provides: "Status cards for scheduling compliance"
    - path: "src/components/compliance/premium-pay-card.tsx"
      provides: "Premium pay calculation display"
    - path: "src/components/compliance/audit-log.tsx"
      provides: "Filterable audit log table"
    - path: "src/app/(dashboard)/compliance/page.tsx"
      provides: "Full compliance page replacing placeholder"
  key_links:
    - from: "src/lib/dal/compliance.ts"
      to: "src/lib/db/schema.ts"
      via: "import auditLog, shifts, users tables"
      pattern: "auditLog"
    - from: "src/lib/dal/compliance.ts"
      to: "src/lib/utils/compliance-rules.ts"
      via: "import calculatePremiumPay, checkNoticePeriod"
      pattern: "calculatePremiumPay|checkNoticePeriod"
    - from: "src/app/(dashboard)/compliance/page.tsx"
      to: "src/lib/dal/compliance.ts"
      via: "server component data fetching"
      pattern: "getComplianceStatus|getAuditLog"
---

<objective>
Build the compliance dashboard at /compliance with predictive scheduling status cards, premium pay calculator, and filterable audit log.

Purpose: Gives managers visibility into scheduling compliance, premium pay exposure, and a full audit trail of all schedule changes.
Output: Working /compliance page with compliance status, premium pay, and audit log.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-shift-swaps-and-compliance/03-CONTEXT.md
@.planning/phases/03-shift-swaps-and-compliance/03-RESEARCH.md
@.planning/phases/03-shift-swaps-and-compliance/03-01-SUMMARY.md
@src/lib/db/schema.ts
@src/lib/utils/compliance-rules.ts
@src/lib/utils/cost-calculator.ts
@src/lib/dal/shifts.ts

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/lib/db/schema.ts (after Plan 01):
```typescript
export const auditLog = pgTable("audit_log", {
  id: integer().primaryKey().generatedAlwaysAsIdentity(),
  action: varchar({ length: 100 }).notNull(),
  userId: integer("user_id").notNull().references(() => users.id),
  details: jsonb().notNull().default({}),
  entityType: varchar("entity_type", { length: 50 }).notNull(),
  entityId: integer("entity_id"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
})
```

From src/lib/utils/compliance-rules.ts (after Plan 01):
```typescript
export interface ComplianceRule { id: string; name: string; description: string; noticePeriodDays: number; premiumPayRules: PremiumPayRule[] }
export interface PremiumPayRule { withinHours: number; premiumHours: number; description: string }
export const DEFAULT_COMPLIANCE_RULES: ComplianceRule
export function calculatePremiumPay(hoursBeforeShift: number, employeeHourlyRate: number, rules?: ComplianceRule): { premiumHours: number; premiumCost: number; rule: string } | null
export function checkNoticePeriod(shiftDate: string, postingDate: string, requiredDays: number): { compliant: boolean; daysNotice: number; requiredDays: number }
```

From src/lib/utils/cost-calculator.ts (existing):
```typescript
export function calculateShiftHours(startTime: string, endTime: string, breakMinutes: number): number
```

From src/lib/dal/shifts.ts (existing pattern):
```typescript
// Pattern: import auth, check session, query with drizzle, return typed result
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compliance DAL and server component page</name>
  <files>src/lib/dal/compliance.ts, src/app/(dashboard)/compliance/page.tsx</files>
  <read_first>
    - src/lib/db/schema.ts (auditLog table structure)
    - src/lib/dal/shifts.ts (DAL pattern to follow)
    - src/lib/utils/compliance-rules.ts (compliance calculation functions)
    - src/lib/utils/cost-calculator.ts (calculateShiftHours for premium pay)
    - src/app/(dashboard)/compliance/page.tsx (current placeholder to replace)
    - src/app/(dashboard)/layout.tsx (layout context)
  </read_first>
  <action>
    1. Create src/lib/dal/compliance.ts with auth-gated queries:
       - `getComplianceStatus()`:
         - Auth check (manager only)
         - Get current week's shifts and next week's shifts
         - For each week, call checkNoticePeriod to determine if schedule was posted on time
         - Return: { currentWeek: { weekLabel, shiftCount, isCompliant, daysNotice, requiredDays }, nextWeek: { weekLabel, shiftCount, isCompliant, daysNotice, requiredDays }, rule: DEFAULT_COMPLIANCE_RULES }

       - `getAuditLog(filters?: { startDate?: string, endDate?: string, employeeId?: number, action?: string, limit?: number, offset?: number })`:
         - Auth check (manager only)
         - SELECT from auditLog JOIN users (for who made the change)
         - Apply optional filters: date range on createdAt, action type on action column, employeeId by checking details JSON or entityId
         - Order by createdAt DESC
         - Default limit 50, support pagination via offset
         - Return: array of { id, action, userName, details, entityType, entityId, createdAt }

       - `getPremiumPayChanges(weekStartDate?: string)`:
         - Auth check (manager only)
         - Get recent audit log entries where action contains "shift." (shift.created, shift.updated, shift.moved, shift.deleted)
         - For each entry, get the affected shift's date/startTime and the employee's hourlyRate
         - Calculate hoursBeforeShift = differenceInHours between auditLog.createdAt and shift start datetime
         - Call calculatePremiumPay(hoursBeforeShift, hourlyRate)
         - Return only entries where premium pay applies (result !== null)
         - Return: array of { auditEntry, shiftDate, shiftTime, employeeName, hourlyRate, premiumHours, premiumCost, rule }

    2. Replace src/app/(dashboard)/compliance/page.tsx:
       - Server component, manager-only access check (redirect to / if not manager)
       - Fetch: complianceStatus, auditLog (latest 50), premiumPayChanges
       - Page title "Compliance Dashboard" with subtitle "Predictive scheduling compliance and audit trail"
       - Layout: two-column grid on large screens
         - Left column: ComplianceStatus cards + PremiumPayCard
         - Right column / below on mobile: AuditLog (full width)
       - Pass data to client components as props
       - Professional indigo-themed design
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/shiftwise && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/dal/compliance.ts contains `export async function getComplianceStatus(`
    - src/lib/dal/compliance.ts contains `export async function getAuditLog(`
    - src/lib/dal/compliance.ts contains `export async function getPremiumPayChanges(`
    - src/lib/dal/compliance.ts contains `await auth()` (auth-gated)
    - src/lib/dal/compliance.ts imports `checkNoticePeriod` from compliance-rules
    - src/lib/dal/compliance.ts imports `calculatePremiumPay` from compliance-rules
    - src/app/(dashboard)/compliance/page.tsx contains `getComplianceStatus` call
    - src/app/(dashboard)/compliance/page.tsx contains `getAuditLog` call
    - src/app/(dashboard)/compliance/page.tsx contains `getPremiumPayChanges` call
    - src/app/(dashboard)/compliance/page.tsx contains `Compliance Dashboard` heading text
    - npx tsc --noEmit has no errors in compliance files
  </acceptance_criteria>
  <done>Compliance DAL provides auth-gated queries for compliance status, audit log, and premium pay. Server component page fetches all data and passes to child components.</done>
</task>

<task type="auto">
  <name>Task 2: Compliance UI components (status cards, premium pay, audit log)</name>
  <files>src/components/compliance/compliance-status.tsx, src/components/compliance/premium-pay-card.tsx, src/components/compliance/audit-log.tsx</files>
  <read_first>
    - src/lib/dal/compliance.ts (data shapes returned by DAL)
    - src/app/(dashboard)/compliance/page.tsx (how components are used)
    - src/components/schedule/shift-card.tsx (existing component patterns)
    - src/lib/utils/compliance-rules.ts (ComplianceRule, PremiumPayRule types)
    - src/lib/utils/schedule-helpers.ts (formatTime helper)
  </read_first>
  <action>
    1. Create src/components/compliance/compliance-status.tsx:
       - Two status cards side-by-side: "Current Week" and "Next Week"
       - Each card shows:
         - Week label ("Mar 16 - Mar 22, 2026")
         - Compliance status: green CheckCircle + "Compliant" or red AlertTriangle + "Non-Compliant"
         - Days notice: "{N} days notice (required: {M})"
         - Shift count: "{N} shifts scheduled"
       - Below both cards: rule info card showing the active compliance rule name, description, and notice period requirement
       - Uses shadcn Card, Badge components
       - Icons: CheckCircle2, AlertTriangle, Shield from lucide-react

    2. Create src/components/compliance/premium-pay-card.tsx:
       - Card showing premium pay exposure
       - Header: "Premium Pay Exposure" with DollarSign icon
       - If no premium pay entries: green "No premium pay triggered" message
       - If entries exist:
         - Total premium cost at top in large text (sum of all premiumCost)
         - List of affected changes:
           - Each row: employee name, shift date/time, change type (from audit action), premium hours, premium cost formatted as currency
           - Rule description text (e.g., "Changes within 24 hours of shift: 2-hour premium pay")
       - Amber/warning styling when premium pay is triggered

    3. Create src/components/compliance/audit-log.tsx:
       - "use client" component for interactivity (filters)
       - Header: "Audit Log" with History icon and total count
       - Filter bar at top with:
         - Action type dropdown (Select): "All", "Shift Created", "Shift Updated", "Shift Deleted", "Shift Moved", "Schedule Copied", "Swap Requested", "Swap Approved", "Swap Rejected", "Pickup Requested", "Pickup Approved"
         - Date range: two date inputs (from/to)
         - Apply/Clear filter buttons
       - Scrollable table/list below filters:
         - Columns: Timestamp (formatted "Mar 22, 3:45 PM"), Action (human-readable label with icon), User (who made change), Details (key info from details JSON)
         - Action labels: map "shift.created" -> "Shift Created" with Plus icon, "shift.updated" -> "Shift Updated" with Pencil icon, "shift.deleted" -> "Shift Deleted" with Trash icon, "shift.moved" -> "Shift Moved" with Move icon, "swap.requested" -> "Swap Requested" with ArrowLeftRight icon, "swap.approved" -> "Swap Approved" with Check icon, "swap.rejected" -> "Swap Rejected" with X icon, "pickup.requested" -> "Pickup Requested" with Hand icon, "pickup.approved" -> "Pickup Approved" with Check icon, "schedule.copied" -> "Schedule Copied" with Copy icon
         - Details column: render key-value pairs from details JSON (date, employee names, times)
       - Client-side filtering (filter the initial data set). If the list grows beyond 50, use "Load More" button that calls a server action to fetch next page.
       - Empty state: "No audit entries found" with filter context
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/shiftwise && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/compliance/compliance-status.tsx contains `Compliant` and `Non-Compliant` text
    - src/components/compliance/compliance-status.tsx contains `CheckCircle` or `CheckCircle2` import from lucide-react
    - src/components/compliance/compliance-status.tsx contains `AlertTriangle` import from lucide-react
    - src/components/compliance/compliance-status.tsx contains `days notice` text
    - src/components/compliance/premium-pay-card.tsx contains `Premium Pay` text
    - src/components/compliance/premium-pay-card.tsx contains `premiumCost` or `premium_cost` reference
    - src/components/compliance/premium-pay-card.tsx contains `DollarSign` import from lucide-react
    - src/components/compliance/audit-log.tsx contains `"use client"`
    - src/components/compliance/audit-log.tsx contains `Audit Log` text
    - src/components/compliance/audit-log.tsx contains filter state (useState for action type or date range)
    - src/components/compliance/audit-log.tsx maps action strings to human-readable labels (contains "Shift Created" or similar)
    - npx next build succeeds without errors
  </acceptance_criteria>
  <done>Compliance page displays compliance status cards (compliant/non-compliant with notice period), premium pay exposure with affected shifts and costs, and a filterable audit log table with human-readable action labels and detail expansion.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- /compliance page renders compliance status cards, premium pay section, and audit log
- Audit log shows action labels with icons
- Compliance status shows compliant/non-compliant indicators
- Premium pay section shows affected changes or "no premium pay" message
</verification>

<success_criteria>
- Compliance status cards show current and next week posting status with compliant/non-compliant indicators
- Warning clearly visible when schedule posted with insufficient notice
- Premium pay calculator shows affected shifts with dollar amounts
- Audit log displays chronological entries with timestamp, user, action, and details
- Audit log filterable by action type and date range
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-shift-swaps-and-compliance/03-03-SUMMARY.md`
</output>
