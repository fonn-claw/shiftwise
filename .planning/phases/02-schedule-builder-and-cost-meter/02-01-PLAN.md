---
phase: 02-schedule-builder-and-cost-meter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/seed.ts
  - src/lib/dal/shifts.ts
  - src/lib/dal/stores.ts
  - src/lib/actions/shifts.ts
  - src/lib/utils/cost-calculator.ts
  - src/lib/utils/schedule-helpers.ts
  - src/lib/utils/cost-calculator.test.ts
  - src/lib/utils/schedule-helpers.test.ts
autonomous: true
requirements: [SCHED-02, SCHED-03, SCHED-04, SCHED-07, SCHED-08, COST-01, COST-02, COST-03, COST-04, COST-05, COST-06, DEMO-03, DEMO-04]

must_haves:
  truths:
    - "Shifts table exists in database with correct columns (employeeId nullable, date, startTime, endTime, roleName, breakMinutes, status, storeId)"
    - "Cost calculator correctly computes total hours, total cost, budget percentage, daily breakdown, per-employee hours, and overtime alerts"
    - "Schedule helper functions correctly compute week ranges, day arrays, time options, and availability checks"
    - "Server actions for shift CRUD validate input and enforce manager-only access"
    - "Seed data creates a fully scheduled current week with Jake Kim at 38 hours and one open Thursday PM cashier shift"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "shifts table + shiftStatusEnum"
      contains: "export const shifts"
    - path: "src/lib/utils/cost-calculator.ts"
      provides: "Pure cost calculation functions"
      exports: ["calculateWeekCosts", "calculateShiftHours", "CostSummary"]
    - path: "src/lib/utils/schedule-helpers.ts"
      provides: "Week navigation and availability utilities"
      exports: ["getWeekRange", "getWeekDays", "formatWeekLabel", "getTimeOptions", "isEmployeeAvailable"]
    - path: "src/lib/dal/shifts.ts"
      provides: "Auth-gated shift data access"
      exports: ["getShiftsForWeek", "getShiftById"]
    - path: "src/lib/actions/shifts.ts"
      provides: "Shift mutation server actions"
      exports: ["createShift", "updateShift", "deleteShift", "copyWeekSchedule"]
    - path: "src/lib/dal/stores.ts"
      provides: "Store data access"
      exports: ["getStore"]
    - path: "src/lib/db/seed.ts"
      provides: "Extended seed with shift data"
      contains: "shifts"
  key_links:
    - from: "src/lib/utils/cost-calculator.ts"
      to: "shifts state array"
      via: "pure function input"
      pattern: "calculateWeekCosts.*shifts.*employees.*budget"
    - from: "src/lib/actions/shifts.ts"
      to: "src/lib/db/schema.ts"
      via: "drizzle insert/update/delete"
      pattern: "db\\.(insert|update|delete)\\(shifts\\)"
    - from: "src/lib/dal/shifts.ts"
      to: "src/lib/db/schema.ts"
      via: "drizzle select"
      pattern: "db\\.select.*from\\(shifts\\)"
---

<objective>
Build the backend foundation for the schedule builder: shifts database table, pure utility functions for cost calculation and schedule helpers, DAL for data access, server actions for shift CRUD, and seed data for a fully scheduled demo week.

Purpose: All UI components in Plans 02 and 03 depend on these data layer primitives. Building them first with tests ensures the UI layer has stable, verified contracts to build against.

Output: Schema extension, tested utility functions, DAL queries, server actions, and enriched seed data.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-builder-and-cost-meter/02-CONTEXT.md
@.planning/phases/02-schedule-builder-and-cost-meter/02-RESEARCH.md

@src/lib/db/schema.ts
@src/lib/db/seed.ts
@src/lib/dal/employees.ts
@src/lib/actions/employees.ts
@src/lib/constants.ts

<interfaces>
<!-- Existing schema types the executor needs -->

From src/lib/db/schema.ts:
```typescript
export const jobRoleEnum = pgEnum("job_role", ["cashier", "stock", "manager", "visual_merch"])
export const stores = pgTable("stores", { id, name, openTime, closeTime, weeklyBudget, timezone, createdAt })
export const users = pgTable("users", { id, email, passwordHash, name, role, hourlyRate, maxHoursPerWeek, phone, storeId, isActive, createdAt, updatedAt })
export const availability = pgTable("availability", { id, userId, dayOfWeek, isAvailable })
```

From src/lib/dal/employees.ts:
```typescript
export type EmployeeWithRoles = {
  id: number; email: string; name: string; role: "manager" | "supervisor" | "employee";
  hourlyRate: string; maxHoursPerWeek: number; phone: string | null; storeId: number;
  isActive: boolean; createdAt: Date; updatedAt: Date;
  jobRoles: { id: number; userId: number; roleName: "cashier" | "stock" | "manager" | "visual_merch" }[];
  availability: { id: number; userId: number; dayOfWeek: number; isAvailable: boolean }[];
}
export async function getEmployees(): Promise<EmployeeWithRoles[]>
```

From src/lib/constants.ts:
```typescript
export const ROLE_COLORS = { cashier: {...}, stock: {...}, manager: {...}, visual_merch: {...} }
export const DAYS_OF_WEEK = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema extension, utility functions with tests, stores DAL</name>
  <files>src/lib/db/schema.ts, src/lib/utils/cost-calculator.ts, src/lib/utils/cost-calculator.test.ts, src/lib/utils/schedule-helpers.ts, src/lib/utils/schedule-helpers.test.ts, src/lib/dal/stores.ts</files>
  <read_first>src/lib/db/schema.ts, src/lib/dal/employees.ts, src/lib/constants.ts, .planning/phases/02-schedule-builder-and-cost-meter/02-RESEARCH.md</read_first>
  <behavior>
    - calculateShiftHours("09:00", "15:00", 0) returns 6
    - calculateShiftHours("09:00", "15:00", 30) returns 5.5
    - calculateShiftHours("09:00", "09:00", 0) returns 0
    - calculateWeekCosts with 2 shifts returns correct totalHours, totalCost, budgetPercent
    - calculateWeekCosts with employee at 36 hours returns amber overtime alert
    - calculateWeekCosts with employee at 41 hours returns red overtime alert
    - calculateWeekCosts skips shifts with null employeeId (open shifts)
    - calculateWeekCosts returns correct dailyCosts keyed by date string
    - getWeekRange returns Monday-Sunday for any date in the week
    - getWeekDays returns array of 7 Date objects starting from Monday
    - formatWeekLabel returns "Mar 16 - Mar 22, 2026" format
    - getTimeOptions("09:00", "21:00") returns 25 entries from "09:00" to "21:00" in 30-min steps
    - isEmployeeAvailable returns false when dayOfWeek has isAvailable=false
    - isEmployeeAvailable returns true when dayOfWeek has isAvailable=true
    - isEmployeeAvailable returns true when no availability record exists for that day (default available)
  </behavior>
  <action>
    1. **Extend schema.ts** — Add shiftStatusEnum and shifts table to existing schema file:
       - `shiftStatusEnum = pgEnum("shift_status", ["assigned", "open"])`
       - `shifts = pgTable("shifts", { id (identity PK), employeeId (nullable FK to users.id with onDelete cascade), date (varchar 10, not null — ISO format "2026-03-16"), startTime (varchar 5, not null — "09:00"), endTime (varchar 5, not null — "15:00"), roleName (jobRoleEnum, not null), breakMinutes (integer default 0, not null), status (shiftStatusEnum default "assigned", not null), storeId (FK to stores.id, not null), createdAt (timestamp defaultNow), updatedAt (timestamp defaultNow) })`
       - Add after existing table definitions. Do NOT modify existing tables.

    2. **Create cost-calculator.ts** — Pure functions (no imports from server modules):
       - Export `CostSummary` interface: `{ totalHours: number, totalCost: number, budgetPercent: number, dailyCosts: Record<string, number>, dailyHours: Record<string, number>, employeeHours: Record<number, number>, overtimeAlerts: { employeeId: number, hours: number, level: "amber" | "red" }[] }`
       - Export `Shift` type used by calculator: `{ id: number, employeeId: number | null, date: string, startTime: string, endTime: string, roleName: string, breakMinutes: number, status: string }`
       - Export `Employee` type used by calculator: `{ id: number, hourlyRate: string, name: string }`
       - Export `calculateShiftHours(startTime: string, endTime: string, breakMinutes: number): number` — parse "HH:mm", compute (end - start - break) / 60, Math.max(0, result)
       - Export `calculateWeekCosts(shifts: Shift[], employees: Employee[], weeklyBudget: number): CostSummary` — iterate shifts, skip null employeeId, lookup employee hourlyRate (parse to Number), accumulate totalHours/totalCost/dailyCosts/dailyHours/employeeHours, compute budgetPercent as (totalCost/weeklyBudget)*100, compute overtimeAlerts for employees >= 35h (amber) and >= 40h (red)

    3. **Create schedule-helpers.ts** — Week navigation and availability utilities:
       - Import from date-fns: startOfWeek, endOfWeek, addWeeks, subWeeks, format, eachDayOfInterval, getDay, parseISO
       - Export `getWeekRange(weekStart: Date): { start: Date, end: Date }` — uses weekStartsOn: 1 (Monday)
       - Export `getWeekDays(weekStart: Date): Date[]` — uses eachDayOfInterval
       - Export `formatWeekLabel(weekStart: Date): string` — returns "Mar 16 - Mar 22, 2026"
       - Export `getNextWeek(weekStart: Date): Date` — addWeeks(startOfWeek, 1)
       - Export `getPrevWeek(weekStart: Date): Date` — subWeeks(startOfWeek, 1)
       - Export `getTimeOptions(storeOpen?: string, storeClose?: string): string[]` — defaults "09:00" to "21:00", 30-min increments, returns array of "HH:mm" strings
       - Export `isEmployeeAvailable(availability: { dayOfWeek: number; isAvailable: boolean }[], date: Date): boolean` — convert JS getDay (0=Sun) to schema convention (0=Mon) via `jsDay === 0 ? 6 : jsDay - 1`, find matching availability record, return isAvailable (default true if no record)
       - Export `formatTime(time: string): string` — convert "09:00" to "9:00 AM", "15:00" to "3:00 PM"

    4. **Create stores DAL** at `src/lib/dal/stores.ts`:
       - Follow same pattern as employees.ts: import auth, db, stores from schema
       - Export `getStore(): Promise<Store>` — auth-gated, returns first store (single-store demo). Store type includes id, name, openTime, closeTime, weeklyBudget, timezone.
       - Throw "Not authenticated" if no session

    5. **Write tests** for cost-calculator and schedule-helpers:
       - cost-calculator.test.ts: Test all behaviors listed above. Use simple mock data (2-3 shifts, 2-3 employees).
       - schedule-helpers.test.ts: Test week range, week days, format label, time options, availability check. Use specific dates (e.g., new Date("2026-03-18") for a Wednesday to verify Monday start).

    Run `npx vitest run --reporter=verbose` to confirm all tests pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils/cost-calculator.test.ts src/lib/utils/schedule-helpers.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const shifts" src/lib/db/schema.ts returns match
    - grep "export const shiftStatusEnum" src/lib/db/schema.ts returns match
    - grep "export function calculateWeekCosts" src/lib/utils/cost-calculator.ts returns match
    - grep "export function calculateShiftHours" src/lib/utils/cost-calculator.ts returns match
    - grep "export interface CostSummary" src/lib/utils/cost-calculator.ts returns match
    - grep "export function getWeekRange" src/lib/utils/schedule-helpers.ts returns match
    - grep "export function isEmployeeAvailable" src/lib/utils/schedule-helpers.ts returns match
    - grep "export function getTimeOptions" src/lib/utils/schedule-helpers.ts returns match
    - grep "export function formatTime" src/lib/utils/schedule-helpers.ts returns match
    - grep "export async function getStore" src/lib/dal/stores.ts returns match
    - All vitest tests pass
  </acceptance_criteria>
  <done>Shifts schema defined, cost calculator and schedule helpers exported with passing tests, store DAL ready</done>
</task>

<task type="auto">
  <name>Task 2: Shift server actions, DAL, and seed data with schedule</name>
  <files>src/lib/actions/shifts.ts, src/lib/dal/shifts.ts, src/lib/db/seed.ts</files>
  <read_first>src/lib/db/schema.ts, src/lib/actions/employees.ts, src/lib/dal/employees.ts, src/lib/db/seed.ts, src/lib/utils/cost-calculator.ts, src/lib/utils/schedule-helpers.ts</read_first>
  <action>
    1. **Create shifts DAL** at `src/lib/dal/shifts.ts`:
       - Import auth, db, shifts from schema, and, gte, lte, eq from drizzle-orm
       - Export `ShiftRow` type matching the shifts table columns: `{ id: number, employeeId: number | null, date: string, startTime: string, endTime: string, roleName: "cashier" | "stock" | "manager" | "visual_merch", breakMinutes: number, status: "assigned" | "open", storeId: number, createdAt: Date, updatedAt: Date }`
       - Export `getShiftsForWeek(weekStart: Date): Promise<ShiftRow[]>` — auth-gated. Compute Monday date string and Sunday date string using `format(date, "yyyy-MM-dd")` from date-fns. Query `db.select().from(shifts).where(and(gte(shifts.date, mondayStr), lte(shifts.date, sundayStr)))`. Return all shifts for the week.
       - Export `getShiftById(id: number): Promise<ShiftRow | null>` — auth-gated. Query by id.

    2. **Create shift server actions** at `src/lib/actions/shifts.ts`:
       - Mark as `"use server"` at top
       - Import z from zod, auth, db, shifts schema, revalidatePath, eq from drizzle-orm
       - Define `createShiftSchema = z.object({ employeeId: z.number().nullable(), date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), startTime: z.string().regex(/^\d{2}:\d{2}$/), endTime: z.string().regex(/^\d{2}:\d{2}$/), roleName: z.enum(["cashier", "stock", "manager", "visual_merch"]), breakMinutes: z.number().int().min(0).max(120).default(0) })`
       - Export `createShift(data)` — auth check (manager only), safeParse, validate endTime > startTime, insert with storeId: 1, status: employeeId ? "assigned" : "open", revalidatePath("/schedule"), return `{ success: true, shift: newShift }` or `{ success: false, message: string }`
       - Export `updateShift(shiftId: number, data)` — same schema + id, auth check (manager only), safeParse, validate endTime > startTime, update where id matches, set updatedAt to new Date(), revalidatePath("/schedule"), return result
       - Export `deleteShift(shiftId: number)` — auth check (manager only), delete where id matches, revalidatePath("/schedule"), return result
       - Export `moveShift(shiftId: number, newEmployeeId: number, newDate: string)` — auth check (manager only), update employeeId and date, set updatedAt, revalidatePath("/schedule"), return result
       - Export `copyWeekSchedule(sourceWeekStart: string, targetWeekStart: string)` — auth check (manager only). Parse source week dates (Monday-Sunday). Query all shifts in source week. For each shift, compute new date by mapping day offset from source Monday to target Monday using date-fns addDays. Insert all new shifts with updated dates. revalidatePath("/schedule"). Return `{ success: true, count: N }`.
       - All actions return `{ success: boolean, message?: string, shift?: ShiftRow, count?: number }`

    3. **Extend seed.ts** to create schedule data:
       - Import shifts from schema and addDays, startOfWeek, format from date-fns
       - After employee creation, add shift seeding section
       - Compute `currentMonday = startOfWeek(new Date(), { weekStartsOn: 1 })`
       - Delete existing shifts before inserting: `await db.delete(shifts)`
       - Create helper: `dateStr(dayOffset: number) => format(addDays(currentMonday, dayOffset), "yyyy-MM-dd")`
       - Seed realistic schedule. Employee lookup by email to get userId. Shift patterns:
         - Sarah Chen (manager): Mon-Fri 9:00-17:00, manager role, 30-min break = 7.5h x 5 = 37.5h
         - Mike Torres (supervisor): Mon-Fri 9:00-17:00, manager role, 30-min break = 37.5h + Sat 9:00-15:00 cashier no break = 6h = 43.5h total (shows overtime red)
         - Wait -- per BRIEF only Jake should be near overtime. Adjust Mike to Mon-Fri only = 37.5h.
         - Emma Wilson: Mon, Wed, Fri 9:00-15:00 cashier = 18h; Tue, Thu 15:00-21:00 cashier = 12h = 30h
         - Jake Kim: Tue-Sat 9:00-15:00 stock (5 x 6h = 30h) + Tue, Thu 15:00-21:00 cashier (2 x 6h = 12h) = wait that is only 42 but Jake max is 25. Need to hit 38 hours. Give Jake: Tue-Fri 9:00-21:00 with 30-min break each = (12h - 0.5h) x 4 = 46? Too much. OK: Tue 9:00-17:00 stock 30min break = 7.5h, Wed 9:00-17:00 stock 30min = 7.5h, Thu 9:00-17:00 cashier 30min = 7.5h, Fri 15:00-21:00 stock = 6h, Sat 9:00-15:00 cashier = 6h, Sun 15:00-18:30 stock = 3.5h. Total = 7.5+7.5+7.5+6+6+3.5 = 38h. Good.
         - Ana Morales: Mon-Thu 9:00-15:00 cashier = 24h
         - Carlos Ruiz: Mon-Fri 15:00-21:00 stock = 30h, Sat 9:00-15:00 stock = 6h = 36h
         - Priya Patel: Wed-Fri 9:00-15:00 cashier = 18h, Sat-Sun 15:00-21:00 stock = 12h = 30h
         - Tom Liu: Wed 15:00-21:00 cashier = 6h, Sat 9:00-15:00 cashier = 6h, Sun 9:00-15:00 cashier = 6h = 18h
         - Maya Johnson: Mon-Fri 9:00-15:00 visual_merch = 30h
         - David Park: Mon-Fri 15:00-21:00 stock = 30h, Sun 9:00-15:00 stock = 6h = 36h
         - Lisa Chen: Thu-Fri 15:00-21:00 cashier = 12h, Sat-Sun 9:00-15:00 cashier = 12h = 24h
         - Ryan O'Brien: Mon-Wed 9:00-15:00 stock = 18h, Thu-Fri 9:00-15:00 visual_merch = 12h, Sat 9:00-15:00 stock = 6h = 36h
         - Open shift (no employee): Thursday 15:00-21:00 cashier, status "open"
       - Log shift count after seeding
       - All shifts use storeId from the created store
       - All breaks default to 0 unless specified (managers get 30-min breaks on full-day shifts)

    Run `npx tsc --noEmit` to verify types compile.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep '"use server"' src/lib/actions/shifts.ts returns match
    - grep "export async function createShift" src/lib/actions/shifts.ts returns match
    - grep "export async function updateShift" src/lib/actions/shifts.ts returns match
    - grep "export async function deleteShift" src/lib/actions/shifts.ts returns match
    - grep "export async function moveShift" src/lib/actions/shifts.ts returns match
    - grep "export async function copyWeekSchedule" src/lib/actions/shifts.ts returns match
    - grep "export async function getShiftsForWeek" src/lib/dal/shifts.ts returns match
    - grep "export type ShiftRow" src/lib/dal/shifts.ts returns match
    - grep "db.delete(shifts)" src/lib/db/seed.ts returns match (shift seeding exists)
    - grep "Jake Kim" seed output would show 38 hours of shifts
    - grep 'status: "open"' src/lib/db/seed.ts returns match (open shift seeded)
    - TypeScript compiles without errors
    - All existing tests still pass
  </acceptance_criteria>
  <done>Shift CRUD server actions with zod validation, shifts DAL with week query, and seed data producing a fully scheduled current week with Jake at 38 hours and one open Thursday PM cashier shift</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass
- `npx tsc --noEmit` — no type errors
- Schema exports shifts table and shiftStatusEnum
- Cost calculator produces correct CostSummary for test inputs
- Schedule helpers handle week boundaries and availability correctly
- Server actions enforce manager-only access and validate shift data
- Seed script includes shift generation for current week
</verification>

<success_criteria>
- shifts table defined in schema with all required columns
- cost-calculator.ts exports calculateWeekCosts and calculateShiftHours with CostSummary interface
- schedule-helpers.ts exports getWeekRange, getWeekDays, formatWeekLabel, getTimeOptions, isEmployeeAvailable, formatTime
- All utility function tests pass
- Server actions for createShift, updateShift, deleteShift, moveShift, copyWeekSchedule exist with zod validation
- Shifts DAL exports getShiftsForWeek and getShiftById
- Stores DAL exports getStore
- Seed script extended to create current week schedule with realistic patterns
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-builder-and-cost-meter/02-01-SUMMARY.md`
</output>
