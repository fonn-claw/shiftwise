---
phase: 02-schedule-builder-and-cost-meter
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/schedule/schedule-builder.tsx
  - src/components/schedule/schedule-grid.tsx
  - src/components/schedule/shift-card.tsx
  - src/components/schedule/week-navigator.tsx
  - src/components/schedule/day-cell.tsx
autonomous: true
requirements: [SCHED-05, SCHED-08, SCHED-09]

must_haves:
  truths:
    - "Manager can drag a shift card to a different employee row or a different day column and the shift is reassigned"
    - "During drag, valid drop targets show green outline and conflict targets show red outline"
    - "Drag is disabled on mobile and for non-manager roles"
    - "Manager can navigate to previous and next weeks via arrows showing date range"
    - "Manager can copy a previous week's schedule to the current week with a confirmation dialog"
    - "Cost meter updates instantly after drag-and-drop shift moves"
  artifacts:
    - path: "src/components/schedule/week-navigator.tsx"
      provides: "Week navigation controls and copy-previous-week button"
      contains: "Copy"
    - path: "src/components/schedule/day-cell.tsx"
      provides: "Droppable day cell with availability-aware feedback"
      contains: "useDroppable"
  key_links:
    - from: "src/components/schedule/shift-card.tsx"
      to: "@dnd-kit/react"
      via: "useDraggable hook"
      pattern: "useDraggable"
    - from: "src/components/schedule/day-cell.tsx"
      to: "@dnd-kit/react"
      via: "useDroppable hook"
      pattern: "useDroppable"
    - from: "src/components/schedule/schedule-grid.tsx"
      to: "@dnd-kit/react"
      via: "DragDropProvider wrapper"
      pattern: "DragDropProvider"
    - from: "src/components/schedule/week-navigator.tsx"
      to: "URL searchParams"
      via: "router.push with week param"
      pattern: "router\\.push.*week="
    - from: "src/components/schedule/schedule-builder.tsx"
      to: "src/lib/actions/shifts.ts"
      via: "moveShift and copyWeekSchedule server actions"
      pattern: "moveShift|copyWeekSchedule"
---

<objective>
Add drag-and-drop shift reassignment using @dnd-kit/react and week navigation with copy-previous-week functionality. This completes the interactive scheduling experience.

Purpose: Drag-and-drop is the signature interaction that makes the schedule builder feel like a modern tool rather than a static table. Week navigation lets managers work across time periods.

Output: Draggable shift cards, droppable day cells with visual feedback, week navigator with prev/next/copy, all wired to existing optimistic update pipeline.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-schedule-builder-and-cost-meter/02-CONTEXT.md
@.planning/phases/02-schedule-builder-and-cost-meter/02-RESEARCH.md
@.planning/phases/02-schedule-builder-and-cost-meter/02-02-SUMMARY.md

@src/components/schedule/schedule-builder.tsx
@src/components/schedule/schedule-grid.tsx
@src/components/schedule/shift-card.tsx
@src/lib/actions/shifts.ts
@src/lib/utils/schedule-helpers.ts
@src/lib/constants.ts

<interfaces>
<!-- From @dnd-kit/react (verified API from research) -->
```typescript
import { DragDropProvider } from "@dnd-kit/react"
import { useDraggable } from "@dnd-kit/react"
import { useDroppable } from "@dnd-kit/react"

// useDraggable returns: { ref, isDragSource }
// useDroppable returns: { ref, isDropTarget }
// DragDropProvider props: { onDragEnd: ({ source, target }) => void, onDragOver?: ({ source, target }) => void }
// source.data and target.data contain the data passed to useDraggable/useDroppable
```

<!-- From Plan 02-02 outputs -->
From src/components/schedule/schedule-builder.tsx:
```typescript
// "use client" component owning shifts state
// Has handleCreateShift, handleUpdateShift, handleDeleteShift
// Needs: handleShiftMove(shiftId, newEmployeeId, newDate) for DnD
// Needs: copyWeekSchedule integration
// Props include: employees, initialShifts, store, weekStart
```

From src/lib/actions/shifts.ts:
```typescript
export function moveShift(shiftId: number, newEmployeeId: number, newDate: string): Promise<{ success: boolean; message?: string }>
export function copyWeekSchedule(sourceWeekStart: string, targetWeekStart: string): Promise<{ success: boolean; count?: number; message?: string }>
```

From src/lib/utils/schedule-helpers.ts:
```typescript
export function formatWeekLabel(weekStart: Date): string
export function getNextWeek(weekStart: Date): Date
export function getPrevWeek(weekStart: Date): Date
export function isEmployeeAvailable(availability: { dayOfWeek: number; isAvailable: boolean }[], date: Date): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drag-and-drop with @dnd-kit/react — draggable shifts, droppable cells, visual feedback</name>
  <files>src/components/schedule/schedule-builder.tsx, src/components/schedule/schedule-grid.tsx, src/components/schedule/shift-card.tsx, src/components/schedule/day-cell.tsx</files>
  <read_first>src/components/schedule/schedule-builder.tsx, src/components/schedule/schedule-grid.tsx, src/components/schedule/shift-card.tsx, src/lib/actions/shifts.ts, src/lib/utils/schedule-helpers.ts, .planning/phases/02-schedule-builder-and-cost-meter/02-RESEARCH.md</read_first>
  <action>
    1. **Install @dnd-kit/react:**
       ```bash
       npm install @dnd-kit/react
       ```

    2. **Create day-cell.tsx** — Droppable cell component:
       - Import useDroppable from "@dnd-kit/react"
       - Import isEmployeeAvailable from schedule-helpers
       - Props: employeeId (number), date (string — ISO date), dateObj (Date), employeeAvailability ({ dayOfWeek: number; isAvailable: boolean }[]), isToday (boolean), children (React.ReactNode), onClick (() => void), isManager (boolean)
       - Call useDroppable with: `{ id: \`cell-${employeeId}-${date}\`, data: { employeeId, date }, disabled: !isManager }`
       - Destructure: `{ ref, isDropTarget }`
       - Compute isAvailable via isEmployeeAvailable(employeeAvailability, dateObj)
       - Build className conditionally:
         - Base: "min-h-16 p-1 border-r border-b border-gray-100 relative group"
         - If isToday: add "bg-indigo-50/50"
         - If !isAvailable: add "border-2 border-red-300 bg-red-50"
         - If isDropTarget AND isAvailable: add "ring-2 ring-green-400 bg-green-50"
         - If isDropTarget AND !isAvailable: add "ring-2 ring-red-400 bg-red-100"
       - Render div with ref and className, children inside, plus a "+" button (opacity-0 group-hover:opacity-100) for empty cells that triggers onClick

    3. **Update shift-card.tsx** — Add draggable behavior:
       - Import useDraggable from "@dnd-kit/react"
       - Add props: isManager (boolean)
       - Call useDraggable with: `{ id: \`shift-${shift.id}\`, data: { shift }, disabled: !isManager }`
       - Destructure: `{ ref, isDragSource }`
       - Apply ref to the card's root div
       - When isDragSource: add "opacity-50 ring-2 ring-indigo-400" classes
       - When isManager: add "cursor-grab" class; when !isManager: "cursor-default"
       - Wrap existing click handler to prevent click during drag: if isDragSource, don't fire onClick

    4. **Update schedule-grid.tsx** — Wrap with DragDropProvider, use DayCell:
       - Import DragDropProvider from "@dnd-kit/react"
       - Import DayCell component
       - Add props: onShiftMove(shiftId: number, newEmployeeId: number, newDate: string), isManager (boolean)
       - Wrap the entire grid in `<DragDropProvider onDragEnd={handleDragEnd}>...</DragDropProvider>`
       - handleDragEnd: `({ source, target }) => { if (!target) return; const shift = source.data.shift; const { employeeId, date } = target.data; if (shift.employeeId === employeeId && shift.date === date) return; onShiftMove(shift.id, employeeId, date) }`
       - Replace inline cell divs with `<DayCell>` components, passing employeeId, date, dateObj, employeeAvailability, isToday, onClick, isManager
       - Pass isManager to ShiftCard components
       - Wrap each EmployeeRow equivalent in React.memo for performance

    5. **Update schedule-builder.tsx** — Add move handler and pass isManager:
       - Add import for moveShift from "@/lib/actions/shifts"
       - Determine isManager: check if the current user session has role "manager". Since this is a client component, receive `userRole` as a new prop from page.tsx. Add `userRole: string` to props.
       - Add `handleShiftMove(shiftId: number, newEmployeeId: number, newDate: string)`:
         - Save previous shifts for rollback
         - Optimistic update: map over shifts, update the matching shift's employeeId and date
         - Call moveShift server action
         - On failure: rollback and toast.error(result.message || "Failed to move shift")
       - Pass onShiftMove={handleShiftMove} and isManager={userRole === "manager"} to ScheduleGrid
       - **Also update page.tsx** to pass `userRole={session.user.role}` from the auth session to ScheduleBuilder. Read session via auth() which is already available in the server component layout.

    6. **Mobile: disable drag-and-drop** (per CONTEXT.md locked decision):
       - In schedule-builder.tsx: detect mobile with a state: `const [isMobile, setIsMobile] = useState(false)` and useEffect with matchMedia("(max-width: 768px)")
       - Pass `isManager={userRole === "manager" && !isMobile}` to ScheduleGrid
       - This disables both useDraggable and useDroppable via the disabled prop on mobile

    Run `npm run build` to verify DnD integration compiles.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep "DragDropProvider" src/components/schedule/schedule-grid.tsx returns match
    - grep "useDraggable" src/components/schedule/shift-card.tsx returns match
    - grep "useDroppable" src/components/schedule/day-cell.tsx returns match
    - grep "onDragEnd" src/components/schedule/schedule-grid.tsx returns match
    - grep "moveShift" src/components/schedule/schedule-builder.tsx returns match
    - grep "ring-green-400" src/components/schedule/day-cell.tsx returns match (valid drop feedback)
    - grep "ring-red-400" src/components/schedule/day-cell.tsx returns match (conflict drop feedback)
    - grep "isDragSource" src/components/schedule/shift-card.tsx returns match
    - grep "isDropTarget" src/components/schedule/day-cell.tsx returns match
    - grep "userRole" src/app/(dashboard)/schedule/page.tsx returns match
    - npm run build succeeds
  </acceptance_criteria>
  <done>Shifts are draggable to different employees/days with green (valid) and red (conflict) visual feedback, optimistic updates with rollback on failure, disabled on mobile and for non-manager roles</done>
</task>

<task type="auto">
  <name>Task 2: Week navigator with prev/next arrows, date range display, and copy-previous-week</name>
  <files>src/components/schedule/week-navigator.tsx, src/components/schedule/schedule-builder.tsx</files>
  <read_first>src/components/schedule/schedule-builder.tsx, src/lib/utils/schedule-helpers.ts, src/lib/actions/shifts.ts, .planning/phases/02-schedule-builder-and-cost-meter/02-CONTEXT.md</read_first>
  <action>
    1. **Create week-navigator.tsx** — Week navigation controls:
       - "use client"
       - Import useRouter from "next/navigation"
       - Import ChevronLeft, ChevronRight, Copy from "lucide-react"
       - Import Button from "@/components/ui/button"
       - Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter from "@/components/ui/dialog"
       - Import formatWeekLabel, getNextWeek, getPrevWeek from schedule-helpers
       - Import format from "date-fns"
       - Import useState from "react"
       - Props: weekStart (string ISO), onCopyWeek (sourceWeekStart: string, targetWeekStart: string) => Promise<void>, isManager (boolean), isCopying (boolean)
       - Parse weekStart to Date
       - Compute prevWeek and nextWeek using getPrevWeek/getNextWeek
       - State: `const [showCopyDialog, setShowCopyDialog] = useState(false)`
       - Navigation: `router.push(\`/schedule?week=${format(targetWeek, "yyyy-MM-dd")}\`)` for prev/next
       - Layout: flex items-center justify-between w-full
         - Left side: prev button (ChevronLeft icon)
         - Center: current week label from formatWeekLabel, bold text
         - Right side: next button (ChevronRight icon) + "Copy Previous Week" button (only visible for managers, shows Copy icon + text)
       - Copy dialog: Dialog with confirmation message "Copy all shifts from {prevWeekLabel} to {currentWeekLabel}? This will add shifts without removing existing ones."
         - Cancel button and "Copy Schedule" button
         - On confirm: call onCopyWeek(format(prevWeek, "yyyy-MM-dd"), format(currentDate, "yyyy-MM-dd")), close dialog
         - While isCopying: disable button, show "Copying..." text

    2. **Update schedule-builder.tsx** — Integrate week navigator and copy:
       - Import WeekNavigator component
       - Import copyWeekSchedule from "@/lib/actions/shifts"
       - Import useRouter from "next/navigation"
       - Add state: `const [isCopying, setIsCopying] = useState(false)`
       - Add handler: `handleCopyWeek(sourceWeekStart: string, targetWeekStart: string)`:
         - setIsCopying(true)
         - Call copyWeekSchedule(sourceWeekStart, targetWeekStart)
         - On success: toast.success(`Copied ${result.count} shifts`), router.refresh() to re-fetch data
         - On failure: toast.error(result.message || "Failed to copy schedule")
         - setIsCopying(false)
       - Render WeekNavigator above the grid/sidebar layout:
         ```
         <div className="space-y-4">
           <WeekNavigator weekStart={weekStart} onCopyWeek={handleCopyWeek} isManager={userRole === "manager"} isCopying={isCopying} />
           <div className="flex gap-6">
             {/* grid and sidebar */}
           </div>
         </div>
         ```
       - Add page header: "Schedule" heading with subtitle above the navigator

    Run `npm run build` to verify everything compiles.
    Run `npx vitest run --reporter=verbose` to confirm all tests still pass.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "ChevronLeft\|ChevronRight" src/components/schedule/week-navigator.tsx returns match
    - grep "formatWeekLabel" src/components/schedule/week-navigator.tsx returns match
    - grep "Copy Previous Week\|Copy Schedule" src/components/schedule/week-navigator.tsx returns match
    - grep "copyWeekSchedule" src/components/schedule/schedule-builder.tsx returns match
    - grep "WeekNavigator" src/components/schedule/schedule-builder.tsx returns match
    - grep "router.push" src/components/schedule/week-navigator.tsx returns match (week navigation)
    - grep "router.refresh" src/components/schedule/schedule-builder.tsx returns match (after copy)
    - npm run build succeeds
    - All vitest tests pass
  </acceptance_criteria>
  <done>Week navigator shows prev/next arrows with date range label, "Copy Previous Week" button with confirmation dialog, week changes update URL and re-fetch data, all existing features still work</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` — all tests pass
- Drag-and-drop works: shift cards are draggable to new cells
- Drop targets show green (valid) or red (conflict) outlines
- Drag disabled on mobile and for non-manager roles
- Week navigation changes URL and loads correct week's data
- Copy previous week duplicates shifts and refreshes the grid
- Cost meter updates after drag-and-drop moves
</verification>

<success_criteria>
- Shift cards can be dragged to different employee rows or day columns
- Valid drop targets show green outline, conflict targets show red outline
- Drag-and-drop is disabled on mobile (< 768px) and for non-manager roles
- Optimistic update on drop with rollback on server failure
- Prev/next week arrows navigate via URL parameter
- Copy Previous Week button opens confirmation dialog and duplicates shifts
- All cost meter displays update correctly after drag operations
- Build passes cleanly, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-builder-and-cost-meter/02-03-SUMMARY.md`
</output>
